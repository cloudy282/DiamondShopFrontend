import React from 'react';
import PropTypes from 'prop-types';

const Shoe = props => {
    return (
        <div>
            Giày
        </div>
    );
};

Shoe.propTypes = {
    
};

export default Shoe;
import React from 'react';
import PropTypes from 'prop-types';

const Clothes = props => {
    return (
        <div>
            Áo quần
        </div>
    );
};

Clothes.propTypes = {
    
};

export default Clothes;